type User {
  id: ID!
  email: String!
  posts: [Post!]
}

type Query {
  getUserById(id: String!): User
  getUserByEmail(email: String!): User
}

type Mutation {
  updateUserEmail(email: String!): Boolean!
}
