type Tag {
  name: String!
  posts: [Post]
  count: Int
}

type Query {
  lookUpTag(input: String!): [Tag]
  findPostsByTagId(tagId: String!): [Post]
}

input tagInput {
  id: String
  name: String!
}
