const tagAndPublishPostSaveError = 'post could not be published'
const genericError = 'something went wrong'
const saveError = 'cannot save to database'
const noUserError = 'cannot find user'
const noPubError = 'cannot find pub'
const userInputError = 'the form is incomplete'

export {
	tagAndPublishPostSaveError,
	genericError,
	saveError,
	noUserError,
	noPubError,
	userInputError,
}